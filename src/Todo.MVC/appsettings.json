{
  "Facebook": {
    "appId": "PULL VALUE FROM ENVIRONMENT",
    "appSecret": "PULL VALUE FROM ENVIRONMENT"
  },
  "Google": {
    "clientSecret": "PULL VALUE FROM ENVIRONMENT",
    "clientId": "PULL VALUE FROM ENVIRONMENT"
  },
  "ConnectionStrings": {
    "TODOConnStr": "PULL VALUE FROM ENVIRONMENT"
  }
}
